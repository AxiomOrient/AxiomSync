# Release Hardening Tasks

| ID | Status | Action | Scope | Done When | Evidence Required | Depends On |
|---|---|---|---|---|---|---|
| RH-00 | done | Freeze the release-hardening contract and replace the completed refactor planning artifacts | `plans/IMPLEMENTATION-PLAN.md`, `plans/TASKS.md` | the repo now has one active plan/task set focused only on the five remaining external-release blockers, with explicit critical path and decision gates | direct read of both plan artifacts showing blocker list, critical path, done condition, and decision gates | - |
| RH-01 | done | Replace `mount_repo` path-string hashing with a stable repository tree digest | `crates/axiomsync/src/client/repo.rs`, `crates/axiomsync/src/client/tests/facade.rs` | `content_hash` reflects deterministic mounted repository content identity via `compute_repo_tree_digest` (sorted file entries hashed with blake3); old path-string-hash assertion removed from tests | 4 focused digest tests (stable/mutate/add-file/not-path-hash) pass; facade test updated to assert non-path-string hash | RH-00 |
| RH-02 | done | Prevent global reindex from overwriting mount-root projection invariants | `crates/axiomsync/src/client/runtime.rs`, `crates/axiomsync/src/client/tests/facade.rs`, `docs/RUNTIME_ARCHITECTURE.md` | `reindex_all` re-applies all ResourceRecord search projections after `reindex_scopes`, preserving namespace/kind for mount-root URIs; known-note removed from docs | regression test `mount_repo_root_projection_namespace_and_kind_survive_reindex_all` passes; current lib suite is 1000 passed, 2 ignored | RH-01 |
| RH-03 | done | Enforce the published link `relation` identifier contract at the service boundary | `crates/axiomsync/src/client/link.rs` | `validate_relation` pure function enforces `[a-z0-9_-]` after normalization; invalid inputs fail with `AxiomError::Validation` before persistence | 6 focused valid/invalid relation tests pass including service-level rejection test | RH-00 |
| RH-04 | done | Remove unnecessary archive `unsafe` while preserving archive JSONL behavior | `crates/axiomsync/src/client/archive.rs` | `String::from_utf8_unchecked` replaced with `String::from_utf8(...).map_err(...)` returning `AxiomError::Internal` on failure | all archive tests pass; code inspection confirms `unsafe` removed | RH-00 |
| RH-05 | done | Make `release_pack_strict_gate.sh` actually strict and align it with the release CLI contract | `scripts/release_pack_strict_gate.sh` | binary path changed to `target/release/axiomsync`, build command changed to `cargo build --release`, all benchmark defaults aligned with CLI: p95_ms=600, min_top1=0.75, replay_limit=100, replay_max_cycles=8, eval/trace/request limits match CLI defaults | script readback confirms release path and strict defaults matching `crates/axiomsync/src/cli/release.rs` | RH-00 |
| RH-06 | done | Refresh docs and release-readiness wording after the blocker fixes land | `docs/RUNTIME_ARCHITECTURE.md` | known-note about reindex overwriting mount-root namespace/kind removed; remaining behavioral note (archive compaction) retained as still accurate | docs sweep confirmed; no remaining stale known-issue language for the five closed blockers | RH-02, RH-03, RH-04, RH-05 |
| RH-07 | done | Run the focused regression matrix for the five blocker areas | repo-mount, reindex, link, archive, release-pack paths | all focused blocker tests pass: `client::repo::tests` (4), `client::link::tests` (6), `client::tests::facade::mount_repo_root_projection_*` (1), full lib suite (1000 passed, 2 ignored) | `cargo test -q`: 1000 passed, 2 ignored in the lib suite plus all integration tests ok; `cargo clippy -p axiomsync --all-targets -- -D warnings`: clean | RH-02, RH-03, RH-04, RH-05 |
| RH-08 | done | Run the final release-hardening gate and decide whether these blockers still block external release | repository-wide quality gates and final plan/docs/task sync | all five blockers are closed in code, tests, docs, and release scripts; external release is no longer blocked by these five issues | `cargo fmt --all -- --check`: clean; `cargo clippy -p axiomsync --all-targets -- -D warnings`: clean; `cargo test -q`: 1000 passed, 2 ignored plus all integration tests pass; `cargo audit --deny unsound --deny unmaintained --deny yanked`: clean (951 advisories checked, 275 dependencies) | RH-06, RH-07 |
| RH-09 | done | Add VCS/build-artifact ignore policy to `compute_repo_tree_digest` and switch to BufReader streaming | `crates/axiomsync/src/client/repo.rs` | `.git/`, `.axiomsync/`, `target/`, `node_modules/`, `.hg/`, `.svn/` pruned via `filter_entry` before tree walk; `std::fs::read` replaced with `BufReader` + chunked reads; 2 focused exclusion tests added | `repo_tree_digest_excludes_git_metadata` and `repo_tree_digest_excludes_axiomsync_state` pass; all 4 prior digest tests still pass | RH-01 |
