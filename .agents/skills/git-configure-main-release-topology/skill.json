{
  "name": "git-configure-main-release-topology",
  "layer": "atomic",
  "layer_badge": "Atomic",
  "default_program": "[stages: preflight>detect>act>verify>handoff | scope: repo | policy: idempotent-setup,safety-first,deterministic-output | output: md(contract=v1)]",
  "required_inputs": [
    {
      "name": "REMOTE_NAME",
      "type": "string",
      "required": false,
      "description": "Remote whose default branch and refs will be inspected. Default: origin."
    },
    {
      "name": "MAIN_BRANCH",
      "type": "string",
      "required": false,
      "description": "Target integration branch name. Default: main."
    },
    {
      "name": "RELEASE_BRANCH",
      "type": "string",
      "required": false,
      "description": "Target deployment branch name. Default: release."
    },
    {
      "name": "SOURCE_BRANCH",
      "type": "string",
      "required": false,
      "description": "Explicit source branch to bootstrap main from. If omitted, detect automatically."
    },
    {
      "name": "SOURCE_DISCOVERY",
      "type": "string",
      "required": false,
      "description": "Source-branch discovery policy. Default: explicit-then-remote-default-then-most-worked."
    }
  ],
  "display_name": "Git / Configure Main Release Topology",
  "browse_category": "git",
  "browse_priority": 5,
  "is_category_default": false,
  "codex_surface": "expert",
  "starter_inputs": []
}
